mutation addComment($postUuid: String!, $username: String!, $Text: String!) {
  addComment(postUuid: $postUuid, username: $username, Text: $Text) {
    comment {
      name
      user {
        username
        photo
      }
      Time
      Text
    }
  }
}